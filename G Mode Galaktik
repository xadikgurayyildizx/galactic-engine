<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>G-MODE • Control the Flow</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <div class="app">

    <header class="hud-header">
      <div id="warpStatus">WARP STATUS: STANDBY</div>
    </header>

    <main class="hud-main">
      <section class="core-section">
        <div class="core-wrapper">
          <div class="core">
            <div class="spiral"></div>
            <div class="energy-core"></div>
          </div>
        </div>
      </section>

      <section class="systems-panel">
        <h2>SYSTEMS PANEL</h2>
        <div class="panel-row">
          <span class="label">
            <span class="icon">┈╹ ))</span>
            Audio Level
          </span>
          <span class="value" id="audioLevel">MID</span>
        </div>
        <div class="panel-row">
          <span class="label">
            <span class="icon">∿</span>
            Pulse Sync
          </span>
          <span class="value" id="pulseSync">ACTIVE</span>
        </div>
        <div class="panel-row">
          <span class="label">
            <span class="icon">⟳</span>
            Color Invert
          </span>
          <span class="value" id="colorMode">PER-PULSE</span>
        </div>
        <div class="panel-row">
          <span class="label">
            <span class="icon">⇄</span>
            Spiral Velocity
          </span>
          <span class="value" id="velocity">MEDIUM</span>
        </div>
        <div class="panel-row">
          <span class="label">
            <span class="icon">●</span>
            Warp Stability
          </span>
          <span class="value" id="stability">OPTIMAL</span>
        </div>
      </section>
    </main>

    <div class="hud-footer">
      <button id="gToggle" class="g-button">
        G • AKTİF ET
      </button>
      <div class="tagline">Control the Flow.</div>
    </div>
  </div>

  <audio id="soundStart" preload="auto">
    <source src="" type="audio/wav">
    <!-- İstersen sonra gerçek bir ses dosyası ekleyebilirsin -->
  </audio>

  <script src="script.js"></script>
</body>
</html>

/* Genel tema: mat siyah + buz mavisi + premium HUD */

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
  background: #02040a;
  color: #cfe6ff;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
    "Roboto", sans-serif;
}

body {
  display: flex;
  justify-content: center;
  align-items: stretch;
}

.app {
  position: relative;
  width: 100%;
  max-width: 480px;
  min-height: 100vh;
  margin: 0 auto;
  padding: 12px 12px 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* Hafif yıldız arka planı */

.app::before {
  content: "";
  position: fixed;
  inset: 0;
  background:
    radial-gradient(circle at 20% 20%, rgba(168, 216, 255, 0.15) 0, transparent 40%),
    radial-gradient(circle at 80% 70%, rgba(168, 216, 255, 0.12) 0, transparent 45%);
  pointer-events: none;
  opacity: 0.18;
}

/* Üst HUD */

.hud-header {
  border: 1px solid rgba(168, 216, 255, 0.4);
  border-radius: 6px;
  padding: 8px 10px;
  text-align: center;
  font-size: 12px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  background: linear-gradient(
    135deg,
    rgba(6, 14, 32, 0.9),
    rgba(4, 10, 24, 0.9)
  );
  box-shadow: 0 0 0 1px rgba(2, 8, 20, 0.8),
    0 0 18px rgba(168, 216, 255, 0.25);
}

/* Ana alan */

.hud-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* Spiral çekirdek alanı */

.core-section {
  border-radius: 12px;
  border: 1px solid rgba(168, 216, 255, 0.35);
  padding: 12px;
  background: radial-gradient(circle at 50% 0, #091021, #02040a);
  box-shadow: 0 0 24px rgba(0, 0, 0, 0.9),
    0 0 28px rgba(168, 216, 255, 0.18) inset;
}

.core-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 16px 4px;
}

.core {
  position: relative;
  width: 200px;
  height: 200px;
}

/* Spiral */

.spiral {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border: 2px solid rgba(168, 216, 255, 0.7);
  box-shadow: 0 0 18px rgba(168, 216, 255, 0.9);
  background:
    conic-gradient(
      from 0deg,
      rgba(168, 216, 255, 0.1),
      rgba(91, 79, 255, 0.55),
      rgba(168, 216, 255, 0.1)
    );
  mix-blend-mode: screen;
  animation: spin 18s linear infinite;
}

/* Pulse sırasında renk invert için */

.app.pulse-active .spiral {
  background:
    conic-gradient(
      from 0deg,
      rgba(91, 79, 255, 0.75),
      rgba(168, 216, 255, 0.35),
      rgba(91, 79, 255, 0.75)
    );
  box-shadow:
    0 0 28px rgba(168, 216, 255, 1),
    0 0 40px rgba(91, 79, 255, 0.8);
}

/* Boş enerji küresi */

.energy-core {
  position: absolute;
  inset: 32px;
  border-radius: 50%;
  background: radial-gradient(circle, #a8d8ff 0, #091021 55%, #02040a 100%);
  box-shadow:
    0 0 12px rgba(168, 216, 255, 0.8),
    0 0 32px rgba(168, 216, 255, 0.45);
  transition: transform 0.2s ease, opacity 0.25s ease;
}

.app.standby .energy-core {
  opacity: 0.6;
}

.app.active .energy-core {
  opacity: 1;
}

/* Pulse ölçeği */

.app.pulse-active .energy-core {
  transform: scale(1.05);
}

/* Systems Panel */

.systems-panel {
  border-radius: 12px;
  border: 1px solid rgba(168, 216, 255, 0.35);
  padding: 10px 10px 12px;
  background: linear-gradient(
    145deg,
    rgba(4, 10, 24, 0.95),
    rgba(3, 8, 18, 0.95)
  );
  box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.85),
    0 0 24px rgba(168, 216, 255, 0.15);
}

.systems-panel h2 {
  font-size: 11px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  text-align: center;
  margin-bottom: 8px;
  color: rgba(168, 216, 255, 0.8);
}

.panel-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 11px;
  padding: 4px 0;
  border-bottom: 1px solid rgba(168, 216, 255, 0.12);
}

.panel-row:last-child {
  border-bottom: none;
}

.label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: rgba(168, 216, 255, 0.8);
}

.icon {
  font-size: 11px;
  opacity: 0.85;
}

.value {
  font-weight: 600;
  color: #e1f0ff;
}

/* Footer / Button */

.hud-footer {
  margin-top: 4px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  align-items: center;
}

.g-button {
  position: relative;
  border: 1px solid rgba(168, 216, 255, 0.75);
  background: radial-gradient(circle at 50% 0, #142238, #050812);
  color: #e1f0ff;
  border-radius: 999px;
  padding: 10px 28px;
  font-size: 13px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  cursor: pointer;
  overflow: hidden;
  box-shadow:
    0 0 18px rgba(168, 216, 255, 0.6),
    0 0 0 1px rgba(0, 0, 0, 0.9);
  transition: transform 0.08s ease, box-shadow 0.12s ease,
    border-color 0.12s ease;
}

.g-button:active {
  transform: scale(0.97);
  box-shadow:
    0 0 10px rgba(168, 216, 255, 0.5),
    0 0 0 1px rgba(0, 0, 0, 0.9);
}

/* Hybrid press ripple */

.g-button::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: radial-gradient(circle, rgba(168, 216, 255, 0.6), transparent 60%);
  opacity: 0;
  transform: scale(0.2);
  pointer-events: none;
}

.g-button.ripple::before {
  opacity: 0.7;
  transform: scale(1.2);
  transition: opacity 0.35s ease, transform 0.35s ease;
}

/* Glow artışı */

.g-button.glow {
  border-color: rgba(168, 216, 255, 1);
  box-shadow:
    0 0 24px rgba(168, 216, 255, 0.9),
    0 0 0 1px rgba(0, 0, 0, 0.9);
}

/* Tagline */

.tagline {
  font-size: 11px;
  color: rgba(168, 216, 255, 0.7);
  letter-spacing: 0.14em;
  text-transform: uppercase;
  text-align: center;
}

/* Durumlara göre sınıflar */

.app.standby #warpStatus {
  color: rgba(168, 216, 255, 0.8);
}

.app.booting #warpStatus {
  color: #a8d8ff;
}

.app.active #warpStatus {
  color: #ffffff;
}

/* Kapanış için hafif kararma */

.app.shutting-down {
  transition: filter 0.4s ease, opacity 0.4s ease;
  filter: grayscale(0.4);
  opacity: 0.92;
}

/* Spiral dönme animasyonu */

@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(1turn);
  }
}

/* Küçük ekran uyumu */

@media (max-width: 360px) {
  .core {
    width: 170px;
    height: 170px;
  }
}

const app = document.querySelector(".app");
const warpStatus = document.getElementById("warpStatus");
const gToggle = document.getElementById("gToggle");

let isActive = false;
let bootTimeouts = [];
let pulseInterval = null;

/* Basınca çalışan ana fonksiyon */
gToggle.addEventListener("click", () => {
  // hybrid ripple + glow animasyonu
  gToggle.classList.remove("ripple", "glow");
  void gToggle.offsetWidth; // reflow hilesi
  gToggle.classList.add("ripple", "glow");
  setTimeout(() => gToggle.classList.remove("ripple"), 200);
  setTimeout(() => gToggle.classList.remove("glow"), 350);

  if (!isActive) {
    startBootSequence();
  } else {
    shutdownSequence();
  }
});

function clearBoot() {
  bootTimeouts.forEach((id) => clearTimeout(id));
  bootTimeouts = [];
}

function startBootSequence() {
  clearBoot();
  clearPulse();

  isActive = false;
  app.classList.remove("standby", "active", "shutting-down");
  app.classList.add("booting");

  warpStatus.textContent = "SYSTEM INITIALIZING...";

  bootTimeouts.push(
    setTimeout(() => {
      warpStatus.textContent = "WARP CORE ONLINE";
    }, 600)
  );

  bootTimeouts.push(
    setTimeout(() => {
      warpStatus.textContent = "SYNCING MODULES...";
    }, 1200)
  );

  bootTimeouts.push(
    setTimeout(() => {
      warpStatus.textContent = "WARP STATUS: ONLINE";
      app.classList.remove("booting");
      app.classList.add("active");
      isActive = true;
      startPulse();
    }, 1900)
  );
}

function startPulse() {
  clearPulse();

  pulseInterval = setInterval(() => {
    // nabız efekti
    app.classList.add("pulse-active");
    setTimeout(() => {
      app.classList.remove("pulse-active");
    }, 180);
  }, 2400);
}

function clearPulse() {
  if (pulseInterval) {
    clearInterval(pulseInterval);
    pulseInterval = null;
  }
  app.classList.remove("pulse-active");
}

function shutdownSequence() {
  clearBoot();
  clearPulse();

  app.classList.add("shutting-down");
  warpStatus.textContent = "WARP STATUS: OFFLINE";

  setTimeout(() => {
    app.classList.remove("active", "shutting-down");
    app.classList.add("standby");
    warpStatus.textContent = "WARP STATUS: STANDBY";
    isActive = false;
  }, 700);
}

/* Başlangıç durumu */
app.classList.add("standby");
